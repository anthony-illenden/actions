name: Run Python Script

on:
  schedule:
    - cron: '*/10 * * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Set up Python
      uses: actions/setup-python@v2
      with:
        python-version: '3.x'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pandas matplotlib numpy

    - name: Run script
      run: python fiveday_forecast.py

    - name: Commit and push changes
      run: |
        git config --local user.email "tonyillenden@gmail.com"
        git config --local user.name "anthony-illenden"
        git add plots/five_day_forecast.png
        git commit -m "Update Five Day Forecast Plot"
        git push
